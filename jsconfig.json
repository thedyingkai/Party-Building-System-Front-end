/**
 * JavaScript 配置文件
 * 用于配置 JavaScript 语言服务，提供更好的代码智能提示和路径解析
 * 
 * @description 配置编译选项和路径别名，支持 @ 符号引用 src 目录
 */
{
  "compilerOptions": {
    "target": "es5",                    // 编译目标为 ES5
    "module": "esnext",                 // 使用最新的 ES 模块系统
    "baseUrl": "./",                    // 基础路径为项目根目录
    "moduleResolution": "node",         // 使用 Node.js 模块解析策略
    "paths": {
      "@/*": [                          // 配置路径别名 @ 指向 src 目录
        "src/*"
      ]
    },
    "lib": [                            // 包含的类型库
      "esnext",                         // 最新的 ES 特性
      "dom",                            // DOM API
      "dom.iterable",                   // 可迭代的 DOM 集合
      "scripthost"                      // Windows Script Host
    ]
  }
}
